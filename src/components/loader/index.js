import { Wrapper } from './wrapper.styled'
import { LoaderComponent } from './loader-component'

const Loader = () => {
  return (
    <Wrapper>
      <LoaderComponent />
    </Wrapper>
  )
}

export { Loader }