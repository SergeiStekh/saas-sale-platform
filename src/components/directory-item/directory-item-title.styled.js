import styled from "styled-components";

const StyledDirectoryItemTitle = styled.p`
  position: relative;
  padding: 5px;
  text-transform: uppercase;
  font-size: 20px;
  font-weight: 700;
  background-color: rgba(255, 255, 255, 0.5);
  border-radius: 8px 8px 8px 8px;
`

export { StyledDirectoryItemTitle }